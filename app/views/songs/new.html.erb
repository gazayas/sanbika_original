<!-- エラーの部分はedit.html.erbの方とかぶっているのでpartialを書いた方がいいかもしれない -->
<%= render partial: "shared/error_partial", locals: { model_instance: @song } %>

<div class="panel panel-default">
  <div class="panel panel-heading">
    <h1><%= t(:new_chart) %></h1>
  </div>
  <%= render 'form', song: @song %>
</div>
