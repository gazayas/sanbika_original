<div id="songs_index" class="container-fluid bg-3 text-center">
  <div class="row">
    <% @songs.each do |song| %>
      <% user = User.find(song[:user_id]) %>
      <div class="col-sm-4 space">
        <%= link_to(song.title, user_song_path(user, song), class: 'btn btn-primary btn-lg') %>　<%# song.artist %>
      </div>
    <% end %>
  </div>
</div>
