<p><%= @resource.name + t(:honorific) + "\n" + t(:mail_greeting) %></p>

<p><%= t(:confirm_link) %></p>

<p>
	<%= link_to t(:confirm_account),
		confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale.to_s)
	%>
</p>
