<p><%= @resource.name + t(:honorific) + "\n" + t(:mail_greeting) %></p>

<p><%= t(:your_account_has_been_locked) %></p>

<p><%= t(:click_to_unlock_message) %></p>

<p><%= link_to t(:click_to_unlock), unlock_url(@resource, unlock_token: @token) %></p>
