<p><%= t('devise.email.greeting', user: @resource.username) %></p>

<p><%= t('devise.email.locked_because_of_too_many_log_in_attempts') %></p>

<p><%= t('devise.email.click_below_to_unlock') %></p>

<p><%= link_to t('devise.email.unlock_my_account'), unlock_url(@resource, unlock_token: @token) %></p>
