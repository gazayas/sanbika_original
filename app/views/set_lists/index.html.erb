<p id="notice"><%= notice %></p>

<h1>Set Lists</h1>

<% if params[:song_id] != nil %>
    <%= render 'select', song_id: params[:song_id] %>
<% else %>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>User</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @set_lists.each do |set_list| %>
          <tr>
            <td><%= set_list.title %></td>
            <td><%= set_list.user_id %></td>
            <td><%= link_to 'Show', user_set_list_path(@user, set_list) %></td>
            <td><%= link_to 'Edit', edit_user_set_list_path(@user, set_list) %></td>
            <td><%= link_to 'Destroy', user_set_list_path(@user, set_list), method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New Set List', new_user_set_list_path(@user) %>
<% end %>
