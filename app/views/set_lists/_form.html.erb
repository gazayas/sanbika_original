<%= form_for([@user, set_list]) do |form| %>
  <% if set_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(set_list.errors.count, "error") %> prohibited this set_list from being saved:</h2>

      <ul>
        <% set_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field text-center">
    <strong>Set List Title</strong>
    <%= form.text_field :title, style: 'font-size: 20px; text-decoration: none;' %>
  </div>

  <!-- Write drag and drop ajax logic here, making sure the order is saved to the database -->
  <!-- link to the set list song itself where the user can change its key and delete it -->
  <!-- Then redirect it back to this page -->
  <div class="set_list_songs text-center">
    <% if !set_list.set_list_songs.empty? %>
      <% set_list.set_list_songs.each do |sls| %>
        <h3><%= link_to Song.find(sls.song_id).title, edit_user_set_list_set_list_song_path(@user, set_list, sls) %></h3>
        <br/>
      <% end %>
    <% end %>
  </div>

  <div class="actions text-center">
    <%= form.submit %>
  </div>
<% end %>
