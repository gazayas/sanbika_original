<p>画像の追加</p>

<%= form_for(@user) do |f| %>

  <% if @user.errors.any? %>
    <div id="image_error" class="panel panel-default">
      <div class="panel panel-heading">
        <h2><%= pluralize(@user.errors.count, "つの問題が発生しました") %></h2><!-- この文字列をlocalesに入れた方が言い -->
      </div>

      <% @user.errors.full_messages.each do |message| %>
        <p class="alert alert-danger image_error_details"><%= message %></p>
      <% end %>
    </div>
  <% end %>

  <div class="form-group">
    <% if @user.user_image? %>
      <div class="thumbnail">
        <%= image_tag @user.user_image.url %>
      </div>
    <% end %>
    <%= f.file_field :user_image %>
    <%= f.hidden_field :user_image_cache %>
  </div>

  <%= f.submit class: 'btn btn-primary', value: '保存', id: 'save_button' %>

<% end %>

